import React from 'react'

function DisplayCalender({ month }) {
  return (
    <div>
      <h2>Calendar for {month}</h2>
      {/* Placeholder for the calendar */}
      <p>Here is the calendar for {month}</p>
    </div>
  );
};
export default DisplayCalender